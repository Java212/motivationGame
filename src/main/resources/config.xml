<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
		https://www.springframework.org/schema/beans/spring-beans.xsd">

    <import resource="events.xml"/>

    <bean id="motivationEventDao" class="ru.inspired.MotivationEventSimpleDao"> <!--назначаем id экземпляру класса -->
        <constructor-arg name="events" ref="events"/>
    </bean>

<!--    <bean id="motivationEventDao" class="ru.inspired.MotivationEventSimpleDao"/>-->

    <bean id="calc" class="ru.inspired.MotivationScoreCalc">  <!--назначаем id экземпляру класса -->
        <!-- конструкторный аргумент "initialScore" передаем значение "0" в конструктор класса "MotivationScoreCalc".
        Это позволяет инициализировать экземпляр класса со значением начального счета. -->
        <constructor-arg name="initialScore" value="0"/>
    </bean>

    <bean id="dailyLogProcessor" class="ru.inspired.DailyLogFileProcessor"> <!--назначаем id экземпляру класса -->
        <!--"motivationEventDao" передам ссылки на другой бин с идентификатором "motivationEventDao"
        в конструктор класса "DailyLogFileProcessor". Это позволяет связать компоненты внутри контекста приложения. -->
        <constructor-arg name="motivationEventDao" ref="motivationEventDao"/>
    </bean>

    <bean id = "motivationChecker" name="motivationChecker" class="ru.inspired.MotivationEventsChecker">
        <constructor-arg name="motivationEventDao" ref="motivationEventDao"/>
        <constructor-arg name="logProcessor" ref="dailyLogProcessor"/>
        <constructor-arg name="calc" ref="calc"/>
    </bean>


</beans>
